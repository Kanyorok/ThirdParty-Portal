version: '3.8'

services:
  nimble:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ERP
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
        - Applications
    ports:
        - "80:80"

networks:
    Applications:
        external: true
