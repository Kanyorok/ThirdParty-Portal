<?xml version="1.0"?>
<ruleset name="BRERP Laravel Coding Standards">
    <description>Custom PHP CodeSniffer rules for BRERP Laravel application</description>

    <!-- What to scan -->
    <file>app</file>
    <file>config</file>
    <file>database/factories</file>
    <file>database/seeders</file>
    <file>routes</file>
    <file>tests</file>

    <!-- How to scan -->
    <arg name="colors"/>
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="parallel" value="80"/> <!-- Enables parallel processing -->
    <arg name="extensions" value="php"/>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>

    <!-- Use PSR-12 as the base standard -->
    <rule ref="PSR12"/>

    <!-- Forbid debug functions and statements -->
    <rule ref="Generic.PHP.ForbiddenFunctions">
        <properties>
            <property name="forbiddenFunctions" type="array">
                <element key="var_dump" value="null"/>
                <element key="print_r" value="null"/>
                <element key="dd" value="null"/>
                <element key="dump" value="null"/>
                <element key="die" value="null"/>
                <element key="exit" value="null"/>
                <element key="eval" value="null"/>
            </property>
        </properties>
    </rule>

    <!-- Detect commented out code -->
    <rule ref="Squiz.PHP.CommentedOutCode">
        <properties>
            <property name="maxPercentage" value="35"/>
        </properties>
    </rule>

    <!-- Detect TODO comments that should be addressed -->
    <rule ref="Generic.Commenting.Todo"/>
    <rule ref="Generic.Commenting.Fixme"/>

    <!-- Ensure proper indentation -->
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="4"/>
            <property name="tabIndent" value="false"/>
        </properties>
    </rule>

    <!-- Warn about complexity -->
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="10"/>
            <property name="absoluteComplexity" value="20"/>
        </properties>
    </rule>

    <!-- Control structure spacing -->
    <rule ref="Squiz.ControlStructures.ControlSignature"/>
    <rule ref="Squiz.WhiteSpace.ControlStructureSpacing"/>
    <rule ref="Squiz.WhiteSpace.ScopeClosingBrace"/>

    <!-- Array formatting -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

    <!-- Line length -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>
</ruleset>
