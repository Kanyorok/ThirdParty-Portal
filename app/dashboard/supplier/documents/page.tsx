import DocsUpload from "@/components/documents/doc-upload"

export default function Documents() {
    return (
        <main className="min-h-screen w-full overflow-x-hidden bg-background">
            <DocsUpload />
        </main>
    )
}